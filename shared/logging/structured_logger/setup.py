from setuptools import setup, find_packages

setup(
    name='structured_logger',
    version='0.1.0',
    packages=find_packages(),
    description='A structured logger for Python',
    install_requires=[
    ],
)
